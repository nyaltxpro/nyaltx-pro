export default function NetworkIcon({ network, variant, size }: { network: string, variant: string, size: string }) {
    return (
        <NetworkIcon network={network} variant="branded" size="14" />
    );
}